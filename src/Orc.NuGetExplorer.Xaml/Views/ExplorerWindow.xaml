<catel:DataWindow x:Class="Orc.NuGetExplorer.Views.ExplorerWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://catel.codeplex.com"
                  xmlns:views="clr-namespace:Orc.NuGetExplorer.Views"
                  xmlns:local="clr-namespace:Orc.NuGetExplorer"
                  xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                  Width="900" Height="500" SizeToContent="Manual"
                  x:ClassModifier="internal">

    <catel:DataWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Orc.NuGetExplorer.Xaml;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>            
        </ResourceDictionary>
    </catel:DataWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <views:RepositoryNavigationView Grid.Row="0" Grid.Column="0" x:Name="NavigationView"/>

        <views:ExtensionsView Grid.Row="0" Grid.Column="1"  SelectedRepository="{Binding SelectedRepository, ElementName=NavigationView}" />

        <GridSplitter Grid.Row="0" Grid.Column="0" Style="{StaticResource VerticalGridSplitterStyle}"
                      HorizontalAlignment="Right" IsEnabled="False"/>
    </Grid>
</catel:DataWindow>
