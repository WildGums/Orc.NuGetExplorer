<catel:DataWindow x:Class="Orc.NuGetExplorer.Example.Views.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://schemas.catelproject.com"
                  xmlns:orccontrols="http://schemas.wildgums.com/orc/controls" 
                  xmlns:orctheming="http://schemas.wildgums.com/orc/theming"
                  ShowInTaskbar="True" SizeToContent="Manual" ResizeMode="CanResizeWithGrip" 
                  WindowStartupLocation="CenterScreen" WindowState="Normal">
    
    <orccontrols:StackGrid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />                             
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <orctheming:ThemeSwitcherView />
            <orctheming:FontSizeSwitcherView />
        </StackPanel>

        <Button Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_Button_Content_Settings}" Command="{Binding Settings}"/>
        <Button Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_Button_Content_ShowExplorer}" Command="{Binding ShowExplorer}" />

        <orccontrols:StackGrid>
            <orccontrols:StackGrid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </orccontrols:StackGrid.RowDefinitions>

            <orccontrols:StackGrid >
                <orccontrols:StackGrid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </orccontrols:StackGrid.ColumnDefinitions>

                <orccontrols:StackGrid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </orccontrols:StackGrid.RowDefinitions>

                <TextBlock Text="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_TextBlock_Text_PackageSourceName}"/>
                <TextBox Text="{Binding PackageSourceName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_TextBlock_Text_PackageSourceFeed}"/>
                <TextBox Text="{Binding PackageSourceUrl, UpdateSourceTrigger=PropertyChanged}"/>
            </orccontrols:StackGrid>

            <Button Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_Button_Content_VerifyFeed}" Command="{Binding VerifyFeed}"/>
            <Button Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_Button_Content_AddPackageSource}" Command="{Binding AddPackageSource}"/>
        </orccontrols:StackGrid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" ItemsSource="{Binding Lines}"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_Button_Content_CheckForUpdates}" Command="{Binding CheckForUpdates}"/>
                <CheckBox Grid.Row="0" Grid.Column="1" Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_CheckBox_Content_AllowPrereleases}" IsThreeState="True" IsChecked="{Binding AllowPrerelease}"/>
                <ListBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding AvailableUpdates}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="{catel:LanguageBinding ResourceName=NuGetExplorerExample_MainWindow_Button_Content_OpenInUpdateWindow}" Command="{Binding OpenUpdateWindow}"/>
            </Grid>
        </Grid>
    </orccontrols:StackGrid>
</catel:DataWindow>
